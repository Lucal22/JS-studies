export default class GeraSenha{
    cliqueBotoes(){
        document.addEventListener('click', function(e){
            const el=e.target;
            if(el.classList.contains('qtd-caracteres')){

            }
    }
        )}
}